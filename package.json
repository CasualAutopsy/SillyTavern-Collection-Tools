{
  "name": "sillytavern-collection-tools",
  "version": "1.0.0",
  "description": "Collection Tools is a libary of SillyTavern macros and slash commands for operations related to lists and dictionaries.",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@babel/preset-env": "^8.0.5",
    "babel-loader": "^10.1.1",
    "terser-webpack-plugin": "^5.6.1",
    "webpack-cli": "^7.2.3"
  }
}
