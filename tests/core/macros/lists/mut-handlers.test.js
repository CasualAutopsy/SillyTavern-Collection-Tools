/**
 * TODO:
 * **Global:**
 * - Test expected return values.
 * - Test console errors with expected empty string return values during them.
 * - Test datatype parsing: strings, booleans, integers, floats, arrays, objects.
 * - Test shorthand variables: scope (@), local (.), global ($).
 *
 * **Notes:**
 * - The scope variable handler is in `./tests/setup-slash.js`.
 * - The local and global variable handlers are in `./tests/setup-macro.js`.
 */
